# 1. Определить тип используемой вами командной оболочки
echo "Тип оболочки: $SHELL"

# 2. Вывести на экран значения переменных среды окружения
echo "Переменные среды окружения:"
echo "HOME: $HOME"
echo "USER: $USER"
echo "SHELL: $SHELL"
echo "PATH: $PATH"

# 3. Перейти в домашний каталог и выдать содержимое файла, созданного в лабораторной работе №1
cd "$HOME" || exit 1
if [ -f "файл_лаб_1.txt" ]; then
    cat "файл_лаб_1.txt"  
    echo "Файл не найден: файл_лаб_1.txt"
fi

# 4. Вывести приглашение пользователю «ввести команду»
printf "Введите команду: "
read user_command

# 5. Проверка ввода команды и выполнение
if [ -z "$user_command" ]; then
    echo "Ошибка: вы не ввели команду."
else
    eval "$user_command"
fi

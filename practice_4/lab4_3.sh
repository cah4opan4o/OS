# Удаляем каталог с фамилией из домашнего каталога
read -p "Введите вашу фамилию для удаления: " surname

# Удаляем подкаталог, если он существует
if [ -d "$HOME/$surname" ]; then
    rm -r "$HOME/$surname"
    echo "Каталог $surname успешно удален."
else
    echo "Каталог $surname не найден."
fi
